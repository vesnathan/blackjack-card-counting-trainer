schema {
  query: Query,
  mutation: Mutation
}

type Mutation {
    saveGame(username: String!, gameData: String!): Game!
    loadGame(username: String!): Game
}

type Game {
    _id: ID! 
    userId: String!
    username: String!
    gameData: String
  }

  type Query {
    games: Game
  }


